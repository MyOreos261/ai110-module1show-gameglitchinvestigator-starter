[flake8]
# The comment prose in this project wraps near 88 columns; 79 would mean
# rewrapping working comments for no readability gain. 100 keeps the real
# findings (spacing, unused imports, undefined names) visible.
max-line-length = 100
exclude = .venv,__pycache__,.pytest_cache
